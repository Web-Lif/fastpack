import React from "react";
import ReactDOM from 'react-dom'


let root = document.createElement("div")
document.body.appendChild(root)

ReactDOM.render(
    <div> hello fastpack</div>
    ,
    root
)
